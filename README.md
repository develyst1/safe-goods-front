# safe-goods-front

Next.js + Ant Design web app for **เว็บกลาง** (safe-goods). Local only; needs `safe-goods-back` on :3001.

1. `cp .env.example .env`
2. `npm install`
3. `npm run dev` → http://localhost:3000 (`/register`, `/login`)
4. `npm run build` / `npm run lint` — the evidence commands
